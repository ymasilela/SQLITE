<Page
    x:Class="TPCWare.SQLiteTest.SearchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TPCWare.SQLiteTest"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBlock HorizontalAlignment="Left" Margin="10,26,0,0" TextWrapping="Wrap" Text="Institution Finder App" VerticalAlignment="Top" FontSize="36" Height="101" FontWeight="Bold" Width="380"/>
        <Button x:Name="back_se" Content="Back" HorizontalAlignment="Left" Margin="10,566,0,0" VerticalAlignment="Top" Width="380" Height="73" Background="#FF27996F" Foreground="#FF060606" Click="back_se_Click"/>
        <ListView x:Name="searchViewList" Margin="20,93,20,96" SelectionChanged="UserList_SelectionChanged">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="10">
                        <TextBlock Text="{Binding Name}" >
                     
                        <Run Text="{Binding ElementName=searchViewList, Path=SelectedItem.Name}" FontWeight="Bold" />
                        </TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</Page>
